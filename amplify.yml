version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Preparing static site for Amplify"
    build:
      commands:
        - echo "No build necessary for static HTML/CSS/JS site"
  artifacts:
    # Amplify will publish the contents of this directory
    baseDirectory: Patient_portal
    files:
      - '**/*'
  cache:
    paths: []
