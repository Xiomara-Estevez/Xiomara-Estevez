name: Brightspace Upload Processor

on:
  push:
    branches:
      - main

jobs:
  process_upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: List uploaded files
        run: |
          echo "Files in repo at HEAD:"
          ls -R
      - name: Example - run tests (placeholder)
        run: |
          echo "Run whatever grading/test scripts you want here"
